#include <iostream>
#include <ostream>
#include <vector>

int main() {
  std::vector<int> vec{1, 3, 2, 4, 6, 5, 7, 8, 9};

  // for (const auto& i : vec) {
  //   std::cout << i << std::endl;
  // }

  // string  接 0 c++

  return 0;
}

// c 的 char 数组 和 int 数组的区别